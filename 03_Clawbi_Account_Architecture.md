# Clawbi｜独立账号与身份架构

> Clawbi 从"工具"升级为"员工"，需要独立的数字身份基础设施。
> 本文档基于 OpenClaw 社区最佳实践，定义 Clawbi 的账号体系、权限架构和安全治理。

---

## 1. 为什么需要独立账号

### 从工具到员工的本质区别

| 维度 | 工具模式（之前） | 员工模式（现在） |
|------|---------------|---------------|
| **身份** | 无身份，通过 API/MCP 调用 | 有名字、有邮箱、有账号 |
| **沟通** | 只在 Terminal 里回答 | 在 Slack/飞书/邮件中像同事一样交互 |
| **权限** | 借用 Erik 的权限 | 独立权限，最小化原则 |
| **可见性** | 团队看不到 AI 在做什么 | 团队能看到 Clawbi 参与讨论 |
| **审计** | 难以追溯 AI 的操作 | 独立操作日志，可追溯 |
| **文化** | "CEO 用了个 AI 工具" | "公司有个 AI 同事叫 Clawbi" |

### 社区共识

OpenClaw 社区（GitHub Discussions、X/Twitter、Substack）的部署经验表明：

> **给 Agent 独立账号是让团队接受 AI 的第一步。**
> 当 AI 有自己的名字、头像和邮箱，团队会自然地把它当作同事而不是工具。

参考案例：
- **Peter Yang 的 "Zoe"**：独立 Gmail + Calendar + 限定 Drive 访问，日常通过 Telegram 交互
- **Bill D'Alessandro 模式**：Google Workspace 独立席位 + 沙盒 VM + 1Password 隔离保险箱
- **Dan Malone 的 "Mission Control"**：多个专业化 Agent 各有独立身份

---

## 2. 账号架构设计

### 2.1 Email — Gmail 独立账号

**实际方案**：`hiclawbi@gmail.com`（独立 Gmail 账号）

**选择 Gmail 而非 Workspace 的原因**：
- 零成本，快速启动
- 独立于公司 Workspace 管理，避免权限复杂性
- Calendar + Drive 功能齐全，够用
- 飞书已覆盖公司域名邮箱（见 2.3）

**权限设计**：

| 功能 | 权限级别 | 说明 |
|------|---------|------|
| **收邮件** | 完整 | 能被 CC/转发到 |
| **发邮件** | 需 Erik 确认 | 草稿 → Erik 审批 → 发送 |
| **Calendar 读** | 只读（被共享的日历） | 查看 Erik 和团队的日程 |
| **Calendar 写** | 自己的日历 + 发邀请需确认 | 可以记录任务，发会邀需审批 |
| **Drive 读** | 限定文件夹 | 只访问被明确共享的文档 |
| **Drive 写** | 限定文件夹 + 审计 | 可在指定文件夹创建/编辑文档 |

**Setup 步骤**：
1. ~~创建 Gmail 账号~~ ✅ `hiclawbi@gmail.com`
2. 设置显示名 "Clawbi 🦀" 和头像
3. 共享 Erik 的 Calendar（只读）
4. 共享指定 Drive 文件夹
5. 在 OpenClaw 中配置 Gmail + Calendar 集成
6. 测试收发邮件、日历查询

---

### 2.2 Slack — Bot Account

**推荐方案**：Slack Bot "Clawbi" 🦀

**为什么是 Bot 而不是 Full Member**：
- 社区共识：**保持 AI 身份透明**，不冒充人类
- Bot 有独立的显示名和标识（"APP" badge）
- 权限更容易控制（通过 Scope 配置）
- 审计更清晰（Bot 的操作可追溯）

**Token 配置**：

| Token 类型 | 用途 | 权限 |
|-----------|------|------|
| **Bot Token (xoxb-)** | 发消息、编辑、反应、上传文件 | 所有写操作 |
| **User Token (xoxp-)** | 读取频道历史、搜索消息 | 扩展读取范围（可选） |

**频道策略**：

| 频道类型 | Clawbi 的权限 | 说明 |
|---------|-------------|------|
| **DM with Erik** | 完整读写 | 主要工作通道 |
| **#general** | 被 @mention 时响应 | 不主动发言 |
| **#ops-channel** | 只读 + 被 @mention 响应 | 关注运营动态 |
| **#clawbi-moments** | 完整读写 | Clawbi 分享有趣交互的专属频道 |
| **其他频道** | 被邀请后参与 | 按需扩展 |

**Setup 步骤**：
1. 在 Slack App 管理中创建 "Clawbi" App
2. 配置 Bot Token 和权限 Scope
3. 设置 Bot 头像（🦀）和 Display Name
4. 在 OpenClaw 中配置 Slack 集成
5. 邀请 Clawbi 到相关频道
6. 测试 DM 和频道交互

---

### 2.3 飞书/Lark — 员工席位（Full Member）

**推荐方案**：飞书/Lark **员工席位**（不是企业应用 Bot）

**为什么选员工席位而不是 Bot**：
- **文化信号**：员工席位让 Clawbi 在飞书通讯录里像真正的同事，强化"AI 员工"定位
- **更自然的交互**：团队成员直接 @Clawbi 或发 DM，跟找同事一样
- **完整权限体验**：可以加入群组、参与审批流、查看日历、使用文档协作
- **与"Make it Human"一致**：买 seat 是在组织层面认真对待 AI 作为同事
- 成本：~$6-15/月（企业版席位费）

**OpenClaw 原生支持飞书**，特性：
- WebSocket 长连接（不需要公网 webhook URL）
- 支持：消息、富文本、卡片、图片、文件、语音、引用、转发
- 支持：云文档、表格、数据库、日历、任务
- 同时支持国内飞书和国际 Lark（通过域名配置切换）

**已注册账号**：
- 主邮箱：`clawbi@cyberflo.ai`
- 邮箱别名：`clawbi@hooraycommerce.com`

**权限设计**（基于员工席位）：

| 功能 | 权限级别 | 说明 |
|------|---------|------|
| **消息收发** | 完整 | DM + 群聊，像普通员工一样 |
| **日历** | 只读（被共享的）+ 写自己的 | 查看团队日程，记录 Clawbi 的任务日历 |
| **云文档** | 被共享的文档可读写 | 只访问被明确共享的文档/表格 |
| **审批流** | 可被分配 + 可评论 | 参与特定审批节点（如数据审核） |
| **通讯录** | 只读 | 了解组织结构 |

**身份透明**：虽然是员工席位，Clawbi 的显示名和个人简介应明确标注为 AI 员工（如 "Clawbi 🦀 | AI Employee #001"），保持 AI 身份透明。

**飞书技术接入 — 员工席位模式**：

> ⚠️ 飞书没有 GitHub 那样的"个人访问令牌"（Personal Access Token）。
> Clawbi 采用**员工席位**模式接入，权限通过飞书管理后台管理，跟管理真人员工一样。
>
> **飞书客户端登录 ≠ OpenClaw 控制权。** 客户端登录是给人（Erik）配置用的；
> OpenClaw 需要 API token 才能以 Clawbi 身份自动操作飞书。

**员工席位模式的优势**：
- **权限管理最简单**：管理员在飞书后台直接操作，不需要配置 API Scopes
- **文档共享**：直接把文档/表格共享给 Clawbi，跟共享给同事一样
- **群组管理**：管理员直接拉 Clawbi 进群，不需要 API 授权
- **日历共享**：共享日历给 Clawbi 员工账号即可
- **审批流**：可以直接把 Clawbi 加为审批节点

**两种 API 身份对比**：

| | tenant_access_token（Bot 模式） | user_access_token（员工模式 ✅） |
|--|------|------|
| **消息来源** | 显示为 Bot（带 APP 标识） | 显示为 Clawbi 员工本人 |
| **权限来源** | 由 API Scopes 配置决定 | 由员工实际被授予的权限决定（所在群、被共享的文档） |
| **体验** | "公司有个机器人" | "公司有个 AI 同事" |
| **适合** | 纯工具型交互 | Clawbi 的"AI 员工"定位 ✅ |

**Setup 步骤**：

*Step 1: 员工账号配置（飞书管理后台 / 客户端）*
1. ~~开通 Clawbi 员工席位~~ ✅ `clawbi@cyberflo.ai`
2. ~~登录飞书客户端~~ ✅ 已登录
3. 设置显示名 "Clawbi 🦀 | AI Employee #001"、头像、个人简介标注 AI 身份
4. 管理员将 Clawbi 拉入相关群组
5. 共享需要的文档/表格/日历给 Clawbi 账号

*Step 2: 创建自建应用 — 给 OpenClaw 的"遥控器"（飞书开放平台）*

> 员工席位是 Clawbi 的"身体"，自建应用是 OpenClaw 的"遥控器"。
> OpenClaw 通过自建应用获取 Clawbi 的 `user_access_token`，以员工身份操作飞书。

6. 登录 [飞书开放平台](https://open.feishu.cn)
7. 创建**自建应用**（企业自建），命名为 "Clawbi Bridge"
8. 在"安全设置"中配置**重定向 URL**（用于 OAuth 授权回调）
9. 申请**用户级别权限**（这些权限决定了 OAuth 授权时 Clawbi 可以授予哪些能力）
10. 提交发布 → 管理员审批上线

*Step 3: OAuth 授权 — 让 Clawbi "授权" OpenClaw 代替自己操作*

> 这一步相当于 Clawbi 对 OpenClaw 说："我授权你用我的身份操作飞书。"

11. 用 Clawbi 的飞书账号完成 OAuth 授权流程
12. 获取 `user_access_token` + `refresh_token`
13. 将 App ID、App Secret、refresh_token 存入 1Password "Shared with Clawbi"
14. 在 OpenClaw 中配置飞书集成

*Step 4: 验证*

15. 测试：OpenClaw 通过 API 以 Clawbi 身份发消息 → 收件人看到的是 Clawbi 员工发的
16. 测试：Clawbi 可以读取被共享的文档
17. 测试：Clawbi 可以在所在群组中发言

> **核心理念**：
> - **飞书客户端** = 给人（Erik）配置和验证用的
> - **自建应用** = 给 OpenClaw 的"遥控器"
> - **user_access_token** = Clawbi 授权 OpenClaw 以自己身份操作
> - **权限** = 由员工席位决定（加群、共享文档），不是由 API Scopes 决定
>
> 日常管理完全在飞书管理后台完成 — 加群、共享文档、调整权限，跟管理真人员工一模一样。

**后续可补充 Bot 能力**（可选）：
- 如果需要 WebSocket 实时接收消息（被 @mention 时自动响应），可以额外启用自建应用的机器人能力
- Bot 负责"听"（接收事件），user_access_token 负责"说和做"（以员工身份操作）
- 这样实现：收消息用 Bot WebSocket，发消息/操作文档用员工身份

**飞书 vs Slack 的选择**：
- 如果团队主要用 Slack → Slack 为主，飞书为辅
- 如果团队主要用飞书 → 飞书为主
- 可以同时配置两个渠道，Clawbi 跨平台响应

---

### 2.4 GitHub — Machine User

**推荐方案**：GitHub Machine User + Fine-grained PAT

| 属性 | 设置 |
|------|------|
| 账号类型 | 独立 GitHub 账号（Machine User） |
| 权限 | Fine-grained Personal Access Token |
| Scope | 限定到特定 Repo（WORK 相关） |
| 操作 | PR Review、Issue Comment、File Read |
| 限制 | 不能 merge、不能 force push |

---

### 2.5 凭证管理 — 1Password 隔离保险箱

**推荐方案**（来自 Bill D'Alessandro 模式）：

1. 在 1Password 中创建 **"Shared with Clawbi"** 保险箱
2. 使用 1Password **Service Account**（不是个人账号）
3. 只放 Clawbi 需要的凭证到这个保险箱
4. Clawbi 通过 Service Account 访问（只能看到这个保险箱）
5. 定期 rotate 凭证

**绝对不放进去的**：
- Erik 的个人密码
- 银行/支付凭证
- 管理员权限 Token

---

## 3. 安全治理

### 3.1 安全现状警告

OpenClaw 社区安全研究发现：
- **30,000+ 暴露的 OpenClaw 实例**被发现可公开访问
- ClawHub Skills 市场 **7.1% (283个 Skills)** 存在安全漏洞
- CyberArk 定义的 "Lethal Trifecta"：访问私有数据 + 暴露于不可信内容 + 代表用户执行操作

> **Clawbi 的安全不能靠"它不会乱来"，要靠硬性控制。**

### 3.2 四层安全架构（OpenClaw 官方推荐）

```
Layer 1: Identity & Access Control（谁能跟 Clawbi 对话）
├── DM Pairing：未知发件人需要审批码
├── Allowlist：预授权的用户列表
└── Per-channel Policy：每个频道独立访问策略

Layer 2: Scope & Authorization（Clawbi 能做什么）
├── Tool Allowlist：明确哪些工具可用
├── Elevated Mode：危险工具需逐次审批
└── Session Authorization：每个 Session 独立授权

Layer 3: Execution Isolation（怎么安全地执行）
├── Container Sandbox：Gateway 运行在 Docker 中
├── Workspace Access：none / ro / rw 分级
└── Network Binding：只绑定 loopback，Tailscale 远程访问

Layer 4: Audit & Response（出问题怎么办）
├── Log Redaction：敏感数据自动脱敏
├── Operation Log：所有关键操作可追溯
└── Secret Rotation：定期更新凭证
```

### 3.3 Clawbi 的具体安全配置

```yaml
# AGENTS.md 安全规则
access_control:
  dm_pairing: true                    # 未知用户需配对
  allowlist: [erik, calvin, kaylie]    # 预授权用户
  group_policy: allowlist             # 群组访问需在列表中

tool_permissions:
  auto_approve:
    - file_read                       # 读文件自动允许
    - web_search                      # 搜索自动允许
    - calendar_read                   # 读日历自动允许
  require_approval:
    - file_write                      # 写文件需确认
    - email_send                      # 发邮件必须确认
    - email_reply                     # 回邮件必须确认
    - shell_exec                      # 执行命令需确认
    - calendar_write                  # 创建日历事件需确认
  blocked:
    - system_modify                   # 禁止修改系统
    - external_api_write              # 禁止调用外部写入 API
    - payment_transaction             # 禁止任何支付操作

escalation_rules:
  financial_threshold: 10000          # >$10K 必须 Erik 审批
  compliance_content: always_human    # 合规内容必须人工审核
  new_vendor_supplier: always_human   # 新供应商必须人工决策
  hr_decisions: always_human          # 人事决策必须人工

execution:
  sandbox: docker                     # Docker 容器隔离
  workspace_access: rw                # WORK 文件夹读写
  network_binding: loopback           # 只绑定本地
  remote_access: tailscale            # Tailscale VPN 远程访问

audit:
  log_redaction: true                 # 日志敏感数据脱敏
  operation_log: true                 # 记录所有操作
  secret_rotation: monthly            # 每月 rotate 凭证
```

### 3.4 未来安全升级路径

| 阶段 | 安全措施 |
|------|---------|
| **Phase 1**（现在） | MacBook 本地运行 + Docker sandbox + 最小权限 |
| **Phase 2**（Month 2） | 迁移到独立 VM/容器 + Tailscale 网络隔离 |
| **Phase 3**（管理层扩展） | 多用户 access control + 按角色权限分级 |
| **Phase 4**（全员推广） | 企业级审计 + 合规框架 + 定期安全评估 |

---

## 4. 实施清单

### Phase 1: 基础账号（Week 1）

- [x] 创建 Gmail 账号 `hiclawbi@gmail.com`
- [ ] 设置 Gmail 显示名 "Clawbi 🦀" 和头像
- [ ] 共享 Erik 的 Calendar（只读）
- [ ] 共享指定 Drive 文件夹
- [ ] 创建 Slack Bot "Clawbi" App
- [ ] 配置 Slack Bot Token 和权限
- [ ] 1Password 创建 "Shared with Clawbi" 保险箱
- [ ] 在 OpenClaw 中配置 Gmail + Calendar + Slack 集成
- [ ] 测试端到端流程：Erik → Slack DM → Clawbi 响应 → 查邮件 → 查日历

### Phase 2: 扩展集成（Week 2-3）

- [x] 飞书员工席位已开通：`clawbi@cyberflo.ai`（主）/ `clawbi@hooraycommerce.com`（别名）
- [x] 飞书客户端已登录
- [ ] 设置飞书显示名 "Clawbi 🦀 | AI Employee #001" 和头像
- [ ] 管理员将 Clawbi 拉入相关群组、共享文档/日历
- [x] 在飞书开放平台创建自建应用 "Clawbi Bridge"（OpenClaw 的"遥控器"）
- [x] 配置 OAuth 重定向 URL + 申请用户级别权限 → 发布上线
- [x] 用 Clawbi 账号完成 OAuth 授权，获取 user_access_token + refresh_token
- [ ] 将凭证存入 1Password "Shared with Clawbi"
- [ ] 在 OpenClaw 中配置飞书集成
- [ ] 测试：Clawbi 以员工身份发消息、读文档、在群组发言
- [ ] （可选）启用 Bot 能力用于 WebSocket 实时接收消息
- [ ] 创建 GitHub Machine User 账号
- [ ] 配置 Docker sandbox
- [ ] 安全自测：尝试让 Clawbi 做不该做的事，确认被拒绝

### Phase 3: 管理层开放（Month 2）

- [ ] 在 access control 中添加 Calvin / Kaylie
- [ ] 为不同用户配置不同权限级别
- [ ] 在团队频道启用 Clawbi @mention 响应
- [ ] 审计 Clawbi 第一个月的操作日志

---

## 参考来源

| 来源 | 内容 |
|------|------|
| [OpenClaw 官方安全文档](https://docs.openclaw.ai/gateway/security) | 四层安全架构 |
| [OpenClaw Slack 集成文档](https://docs.openclaw.ai/channels/slack) | Slack Bot 配置 |
| [OpenClaw 飞书集成文档](https://docs.openclaw.ai/channels/feishu) | 飞书 Bot 配置 |
| Bill D'Alessandro (X/Twitter) | VM sandbox + 1Password 模式 |
| Peter Yang "Zoe" (Substack) | 独立 Gmail + Calendar 模式 |
| CyberArk 安全研究 | Agent 安全风险分析 |
| Snyk 安全研究 | Skills 市场安全漏洞 |
| MMNTM 架构分析 | SOUL.md / IDENTITY.md 设计理念 |

---

**Document Owner**: CEO
**Created**: 2026-02-10
**Version**: v0.1 (Draft)

> *"给 Clawbi 一个邮箱，不只是技术配置。*
> *是在告诉团队：我们认真地把 AI 当作同事。"*
>
> **🦀 clawbi@cyberflo.ai — Ready to work.**
